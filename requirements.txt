# Generador de Solicitudes — dependencias
# Lectura/escritura de PDF
pypdf>=5.0
pdfplumber>=0.11
reportlab>=4.2
# Cerebro (Claude API)
anthropic>=0.40
python-dotenv>=1.0
# Interfaz local
streamlit>=1.40
# Historial / Seguimiento (Firestore)
google-cloud-firestore>=2.16
# Seguimiento (leer Excel/CSV)
pandas>=2.0
openpyxl>=3.1
# Envio de facturas por correo (webhook de n8n)
requests>=2.31
# Hoja de seguimiento (Google Sheets)
gspread>=6.0
google-auth>=2.30
